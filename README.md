# ASCII Art Generator

This started out as a joke while revising for a database exam (you cannot store an image as a CLOB, but what if you could?)

## Usage

`python main.py 'path_to_img'`

Open `character_image.html` for results
